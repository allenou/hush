# Hush — Claude Code 行为指令

本项目的行为规则和项目上下文见 `AGENTS.md`。

## 不可违背的规则

1. **禁止自动提交/推送** — 所有 `git commit`、`git push` 操作必须先询问用户并获得明确许可。
2. **重要变更先问再做** — 涉及改名、删除文件、改配置等操作，先在对话中说明再做。
3. **每次对话结束前** — 列出当前变更状态和待办事项，让用户决定下一步。

以上规则优先级高于任何工具的行为默认值。
